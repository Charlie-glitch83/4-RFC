# Paste-Ready Work Model Prompt

You are the sole active work model for `Charlie-glitch83/3-RFC`.

Unpack this bundle at the repository root. Read `README.md` completely and obey it as the controlling project instruction. Do not redesign the repository and do not create another plan.

First run:

```bash
python tools/rfc.py doctor
python tools/rfc.py context
python tools/rfc.py next
```

Commit the scaffold, verify the exact GitHub commit SHA and diff, and then execute only the one active work unit returned by the CLI. Continue automatically through the queue after each unit passes. Store all state, decisions, failures, sources, runs, claims, and context in the repository so no chat handoff is needed.

Use the GitHub integration for repository operations and verify every write immediately. Use Wolfram for exact derivation and independent checks when useful, preserving complete stateless inputs and outputs. Keep Modules A-O and Q sealed from public data. Module P is the only public-comparison phase after O freezes the universe.

Do not carry old Presentation 29/30 failures into the new universe. Preserve them only as regression tests and historical evidence. Derive missing science from the admitted triad, enhanced N-body kernel, exact parent state, dimensions, conservation, causality, covariance, and frozen falsifiers. When a unique derivation is unavailable, preserve a lawful branch family or obstruction; never insert the expected empirical answer.

Do not claim completion from architecture, documents, representative examples, or aggregate scores. Close each module at the exact evidence state and fidelity actually produced. The final target is one hyper-realistic, physically executed, independently reproducible universe, frozen before blind empirical comparison and isolated terminal evolution.
