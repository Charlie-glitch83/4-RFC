# Sources

`source_seed/` is supplied material and remains unadmitted until hashed and classified by `SRC-010`.

`frozen/` contains immutable admitted bytes under content-addressed directories.

`quarantine/` contains conflicting, contaminated, incomplete, or unverified objects.

Use `python tools/rfc.py admit-source` rather than manually copying into `frozen/`.
