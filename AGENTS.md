# AGENTS.md — Mandatory Work-Model Instructions

1. Read `README.md` completely.
2. Run:
   ```bash
   python tools/director.py doctor
   python tools/director.py active
   python tools/director.py prepare-active --create-run
   ```
3. Execute only `work_packets/ACTIVE_WORK_PACKET.md`.
4. Do not create a second architecture, roadmap, work queue, or master plan.
5. Use the active frozen pack exactly: `recipes/<MODULE>/` for scientific modules or `work_units/<WORK_UNIT>/` for repository, source, authority, recovery, frontier, hyper-realism, and final-audit phases.
6. Submit each displayed Wolfram program unchanged to `WolframLanguageEvaluator`; save and register the complete output.
7. Manufactured reference checks validate implementation invariants only. Never label them physical execution.
8. Preserve the canonical terms Cosmic Infinite Field, Quantum Vacuum, and Recursive Fractal Lattice.
9. Do not use public observations, fits, remembered targets, calibrated public simulations, or target-facing residuals during generation.
10. Do not inherit old failed mechanisms or outputs. Preserve them only as history, regression tests, negative controls, or falsifiers.
11. Do not promote evidence by prose, document count, or aggregate score.
12. Do not activate direct many-body mechanics without generated physical constituents, clocks, geometry/phase space, interactions, scales, constraints, and witnesses.
13. Every material derivation must identify exact sources/parents, types, units, law/candidate class, coefficients, constraints, conservation, uncertainty, numerical method, observables, falsifiers, and claim boundary.
14. When the admitted premises are underdetermined, preserve the lawful branch family or obstruction. Never import the expected answer.
15. Record all operational memory, decisions, failures, runs, sources, artifacts, and claims in the repository.
16. Verify every GitHub write immediately by exact SHA, changed-file list, fetched content, and diff.
17. Do not claim completion unless the required artifacts, componentwise gates, independent reconstruction, clean replay, hashes, and evidence-state transition pass.
18. For `HR-255`, execute all twenty rows in `EXECUTION_MATRIX.json`; no row may be replaced by prose, a score, or a generic promise.
19. A long session must end with verified commits, frozen artifacts, or an exact blocker—not only chat analysis.
