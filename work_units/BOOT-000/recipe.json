{
  "schema_version": "1.0",
  "work_unit": "BOOT-000",
  "title": "Install, Verify, and Commit the Repository Scaffold",
  "module": "REPO",
  "objective": "Place this scaffold at the root of Charlie-glitch83/3-RFC, run all local checks, commit it, and verify the exact GitHub SHA and diff.",
  "depends_on": [],
  "exact_commands": [
    "bash bootstrap.sh",
    "python tools/director.py doctor",
    "python -m unittest discover -s tests -v",
    "python tools/rfc.py firewall-scan"
  ],
  "exact_sequence": [
    "Copy the complete bundle to the empty repository root.",
    "Commit the scaffold exactly once.",
    "Fetch README.md and the commit through GitHub, verify SHA and diff, then record the commit.",
    "Copy the bundle contents into the empty 3-RFC repository without redesigning them.",
    "Run python tools/rfc.py doctor, python -m unittest discover -s tests -v, and python tools/rfc.py firewall-scan.",
    "Create work branch agent/3rfc-universe-build after the scaffold is visible on the default branch or as the initial branch, according to repository state.",
    "Commit with the prescribed message and verify the commit SHA, file list, fetched README, and branch comparison.",
    "Record the verified SHA in memory/DECISION_LOG.jsonl and the closeout."
  ],
  "deliverables": [
    "runs/BOOT-000/CLOSEOUT.md",
    "verified GitHub commit SHA",
    "passing doctor/tests/firewall scan"
  ],
  "componentwise_gates": [
    "README visible in GitHub",
    "new commit SHA exists",
    "diff contains scaffold",
    "doctor and tests pass"
  ],
  "commit_message": "Initialize the 3-RFC governed universe workspace",
  "hard_stops": [
    "required source or parent hash is missing",
    "a public target would influence a generation decision",
    "a claimed artifact cannot be fetched and independently verified",
    "a componentwise gate fails"
  ]
}
