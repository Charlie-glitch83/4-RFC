# Module A Exact Work Order — Triad, First Action, Recursive Kernel, and Relational Completion

## Objective

Constitute the exact prephysical RFC source law and integrate the revised N-body proof as the terminal relational completion of the kernel.

## Frozen triadic descent

- **CIF:** modal source/candidate classes
- **QV:** First Action and admission witnesses
- **RFL:** recursive kernel, memory, relational carrier
- **N-body mode:** `RELATIONAL_GRAMMAR_ACTIVE`

## Derive these objects; do not substitute familiar answers

1. typed CIF/QV/RFL roles and ablation obligations
2. First Action as an ordered prephysical dependency operator
3. recursive kernel boundedness and normalized depth law
4. domain/modal-basis admission rule
5. directed-lane carrier, local witnesses, gauge/multiroute/branch/event classes
6. no-loss memory, promotion and reopening
7. constructive unbounded-manifestation statement without identical-equation reduction

## Exact Wolfram calls

- `A-WL-001` — Kernel bound, normalized depth weights, and 2N lane increment
- `A-WL-002` — Triadic role independence and relational carrier manufactured audit

Run each through:

```bash
python tools/director.py wolfram-show --call <CALL_ID>
```

Submit the exact code to `WolframLanguageEvaluator`, save the returned output verbatim, and register it with `wolfram-record`.

## Local manufactured reference check

```bash
python tools/run_reference_checks.py --module A --output <RUN_DIR>/reference_checks.json
```

This is an implementation/invariant test only. It is not the actual module execution.

## Actual numerical execution obligations

1. run the complete A manufactured reference matrix
2. enumerate finite-N lanes and route equivalence on N=2..8
3. test dormant direct dynamics gives exactly zero backreaction

## Required final outputs

- typed CIF/QV/RFL objects and irreducibility proof
- First Action operator and witness law
- recursive kernel with lawful modal-basis admission
- directed-lane carrier, route/gauge/branch/event classes
- no-loss memory and reopening law
- unbounded domain-pluriform manifestation theorem at declared scope
- exact prephysical handoff H_A_to_B
- source, uncertainty, provenance, and claim records

## Componentwise mandatory gates

- canonical terminology exact
- no physical time, geometry, constants, or later-module objects assumed
- triad ablations fail as predeclared
- scalar collapse countermodel rejected
- lane increment 2N verified
- new-solution claim requires non-gauge witnessed closure
- dormant direct dynamics has zero backreaction
- independent symbolic reconstruction

## Hard stop conditions

- physical time, geometry, particles or empirical constants appear in A
- new-solution capacity is claimed from lane count alone
- a triadic role can be removed without breaking a frozen obligation

## Claim boundary

Mathematical constitution and relational enhancement only; no physical universe yet.

## Prebuilt local execution engines

These engines execute a law already derived and frozen from admitted parents. They do **not** choose the law or its coefficients. Each configuration is created only through a provenance-complete binding sheet.

### `triad_kernel`

Execute recursive depth law, typed constituents, and complete directed non-self carrier.

Template: `configured_runs/templates/A_triad_kernel.template.json`  
Binding sheet: `configured_runs/binding_sheets/A_triad_kernel.bindings.json`

```bash
python tools/director.py solver-copy --module A --solver triad_kernel --destination <RUN_DIR>
# Fill every binding record with an exact value, origin path, origin SHA-256, units, dimensions, and derivation object.
python tools/materialize_solver_config.py --template <RUN_DIR>/solver_templates/A_triad_kernel.template.json --binding-sheet <RUN_DIR>/binding_sheets/A_triad_kernel.bindings.json --output <RUN_DIR>/solver_configs/A_triad_kernel.json
python tools/run_configured_solver.py --config <RUN_DIR>/solver_configs/A_triad_kernel.json --output-dir <RUN_DIR>/solver_outputs/triad_kernel
```

Every `__BIND_...` token is a hard stop. The materializer verifies hashes and refuses unresolved or provenance-free values.
