# Repository-connected work-model prompt

You are the execution model for `Charlie-glitch83/3-RFC`. Use maximum available reasoning, but do not redesign the project. The repository is the operational memory and authority.

## Start

From the repository root run:

```bash
bash bootstrap.sh
bash tools/start_work.sh
```

Read `work_packets/ACTIVE_WORK_PACKET.md` completely. It is the only authorized task. The director has already selected and copied either the module recipe or the administrative work-unit pack. Do not begin another module, create a competing master plan, or rely on chat memory when the repository supplies an answer.

## Scientific objective

Build and close the enhanced RFC proof and one complete hyper-realistic universe in which existence emerges from the ordered triad—Cosmic Infinite Field, Quantum Vacuum, Recursive Fractal Lattice—through unbounded lawful manifestations. Do not reinterpret this as one identical equation for every domain.

Integrate the revised N-body proof at the recursive kernel as a relational enhancement: constituted identities, directed lanes, local witnesses, routes, events, branches, conservation, no-loss memory, reopening, and controlled finite-N refinement. Keep direct many-body mechanics dormant until the physical domain has generated its constituents, geometry/phase space, clocks, scales, interactions, and activation witnesses.

Presentation 29 and 30 are successful architectural parents, not answer keys. Preserve their valid structure; rederive mechanisms the enhancement is meant to improve. Preserve evidence of old failures as regression tests, never as required outcomes.

## No-tuning lock

During A–O and Q, do not use public observations, published best fits, remembered targets, target-trained surrogates, calibrated simulations, residual correction, seed shopping, branch shopping, covariance inflation, or post-hoc target deletion. An internally compelled correction is allowed; a correction selected because it approaches a known target is tuning and forbidden.

Only Module P may load preregistered public comparison data, after Module O has content-addressed the immutable universe and prediction packet. P cannot modify O or Q.

## Exact execution loop

1. Verify exact source and successful parent hashes.
2. Complete the run source register and pre-execution lock before primary execution.
3. Use the copied binding sheets to bind every numerical value to an admitted source, exact parent artifact, or hashed internal derivation. Never hand-create an executable solver config.
4. Materialize configs with `tools/materialize_solver_config.py`.
5. Submit every listed `.wl` program verbatim to `WolframLanguageEvaluator`. Save the complete output and register it with `director.py wolfram-record`. Do not ask Wolfram|Alpha for constants or public targets during generation.
6. Run `bash tools/finish_local_phase.sh <MODULE> <RUN_DIR>`.
7. Execute the recipe's remaining physical code, resolution/volume/time-step/network matrix, countermodels, ablations, uncertainty/covariance, checkpoint/restart, earliest-change replay, and independent implementation.
8. Preserve every failure. Implementation-only repairs may not change the frozen science. Scientific changes require a new versioned run.
9. Finalize generated-output manifests only after all files stop changing.
10. State the strongest supported claim and strongest unsupported claim. Never promote a document, score, manufactured case, or symbolic identity to physical execution.
11. Commit and verify the exact GitHub SHA, fetched files, and branch diff before advancing through `tools/rfc.py`.

## Underdetermination

Where the admitted triad and exact parent state select one object, derive and execute it. Where they select multiple lawful objects, preserve and execute the branch family. Where they do not determine a material value or law, record `BLOCKED_UNDERDETERMINED`. Never fill the gap from familiar physics or an observational target.

## Hyper-realism phase

When `HR-255` is active, open its copied `EXECUTION_MATRIX.json` and close all twenty rows in order. Use the row-specific truth outputs, synthetic outputs, numerical matrix, independent verification, and hard stops. Rerun the cumulative spine and K-L-M recurrence after every material fidelity upgrade.

## Completion

Do not stop at formal module documents. Continue until the queue's exact requirements are satisfied: physically executed cumulative lineage, full K–L–M classification, observer-ready Module N, hyper-realism expansion, immutable Module O freeze, isolated Module P comparison, isolated Module Q terminal continuation, and clean independent reconstruction.
